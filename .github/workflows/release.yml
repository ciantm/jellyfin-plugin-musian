name: Build & Release

on:
  push:
    branches:
      - main
    paths:
      - 'build.yaml'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      artifact: ${{ steps.version.outputs.artifact }}
      checksum: ${{ steps.pkg.outputs.checksum }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Parse version from build.yaml
        id: version
        run: |
          VER=$(grep '^version:' build.yaml | sed 's/version: *"\(.*\)"/\1/')
          TAG="v${VER}"
          ARTIFACT="Jellyfin.Plugin.Musian_${VER}.zip"
          echo "tag=$TAG"           >> "$GITHUB_OUTPUT"
          echo "version=$VER"       >> "$GITHUB_OUTPUT"
          echo "artifact=$ARTIFACT" >> "$GITHUB_OUTPUT"

      - name: Build
        run: |
          dotnet build \
            Jellyfin.Plugin.Musian/Jellyfin.Plugin.Musian.csproj \
            --configuration Release \
            -p:Version=${{ steps.version.outputs.version }} \
            --output ./build_output

      - name: Package
        id: pkg
        run: |
          ARTIFACT="${{ steps.version.outputs.artifact }}"
          cd build_output
          zip "../${ARTIFACT}" Jellyfin.Plugin.Musian.dll
          cd ..
          echo "checksum=$(md5sum "${ARTIFACT}" | awk '{print $1}')" >> "$GITHUB_OUTPUT"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin-zip
          path: ${{ steps.version.outputs.artifact }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          # Explicit token: the action otherwise has to discover it, and the
          # "finalize release" call has been seen to fail with `HttpError:
          # Server Error` / "Too many retries" right after the release is
          # created — which then skips the manifest job below even though the
          # release and its asset exist. Passing the token directly avoids the
          # discovery step.
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ steps.version.outputs.tag }}
          name: Musian ${{ steps.version.outputs.version }}
          # Never leave a half-finished release behind on a retry.
          draft: false
          make_latest: true
          body: |
            ## Musian ${{ steps.version.outputs.version }}

            ### Install via Jellyfin
            Add this manifest URL to Jellyfin → Dashboard → Plugins → Repositories:
            ```
            https://ciantm.github.io/jellyfin-plugin-musian/manifest.json
            ```
            Then search for **Musian** in the plugin catalogue.

            ### Manual install
            Download `${{ steps.version.outputs.artifact }}`, extract the DLL into your Jellyfin plugins folder and restart.

            **MD5:** `${{ steps.pkg.outputs.checksum }}`

            ---

            📱 **Want Musian on your phone?** The Android app adds background playback,
            Android Auto and offline support — [get it on Google Play](https://play.google.com/store/apps/details?id=com.musian.app)
            (the app is a paid upgrade; this Jellyfin plugin is free and fully featured).
          files: ${{ steps.version.outputs.artifact }}
          fail_on_unmatched_files: true

  update-manifest:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          fetch-depth: 0

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: plugin-zip

      - name: Compute timestamp
        id: meta
        run: |
          echo "timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - name: Update manifest.json
        run: |
          python3 update_manifest.py \
            --version   "${{ needs.build.outputs.version }}" \
            --tag       "${{ needs.build.outputs.tag }}" \
            --checksum  "${{ needs.build.outputs.checksum }}" \
            --timestamp "${{ steps.meta.outputs.timestamp }}"

      - name: Commit & push manifest
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add manifest.json
          git commit -m "chore: update manifest for ${{ needs.build.outputs.tag }}" || echo "No changes"
          git push

      # Pages source is now "GitHub Actions" (switched 2026-08-06 after the legacy
      # branch-deploy builder started failing outright — see git history for context).
      # Committing manifest.json to gh-pages above is kept for history/audit trail,
      # but no longer auto-publishes anything by itself; these steps do the actual deploy.
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          # Creates the Pages site if it does not exist yet. Without this the
          # deploy step fails on a freshly created repository.
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
